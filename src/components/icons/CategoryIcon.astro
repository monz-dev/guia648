---
import { Map, UtensilsCrossed, HeartPulse, Wrench } from '@lucide/astro';
import type { AstroComponentFactory } from 'astro/runtime/server/ast.js';

interface Props {
  icon: string;
  class?: string;
}

const { icon, class: className } = Astro.props;

const icons: Record<string, AstroComponentFactory> = {
  'map': Map,
  'utensils': UtensilsCrossed,
  'heart-pulse': HeartPulse,
  'wrench': Wrench,
};

const IconComponent = icons[icon] || Map;
---

<IconComponent class={className} />
